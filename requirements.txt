# Core runtime deps for Jetson-Live-Translator.
#
# Notes:
# - You still need system packages like `ffmpeg` (Whisper) and PortAudio (PyAudio).
# - On Jetson, install NVIDIA-provided PyTorch + TensorRT (JetPack) first; then `pip install -r requirements.txt`.
#
# If you vendor these repos instead, you can replace the git URLs with `-e ./whisper_trt` and/or `-e ./torch2trt`.

whisper_trt @ git+https://github.com/NVIDIA-AI-IOT/whisper_trt.git
torch2trt @ git+https://github.com/NVIDIA-AI-IOT/torch2trt.git

openai-whisper

# Optional backends / pipeline components (keep if you use them).
faster-whisper
ctranslate2

# Runtime libraries used by whisper_trt + examples.
numpy
psutil
onnx
onnxruntime
pyaudio
soundfile
